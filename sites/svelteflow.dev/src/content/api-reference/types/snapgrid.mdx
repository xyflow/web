---
description: The SnapGrid type is used to define the grid size for snapping nodes on the pane.
---

# SnapGrid

The SnapGrid type is used to define the grid size for snapping nodes on the pane.

```ts
type SnapGrid = [number, number];
```
