---
title: The Background component
sidebarTitle: '<Background />'
description: 'The Background component makes it convenient to render different types of
  backgrounds common in node-based UIs. It comes with three variants: lines,
  dots and cross.'
---

# &lt;Background />

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/svelte/src/lib/plugins/Background/Background.svelte)

The `<Background />` component makes it convenient to render different types of
backgrounds common in node-based UIs. It comes with three variants: `lines`,
`dots` and `cross`.

```svelte
<script lang="ts">
  import { SvelteFlow, Background, BackgroundVariant } from '@xyflow/svelte';

  let nodes = $state.raw([]);
  let edges = $state.raw([]);
</script>

<SvelteFlow bind:nodes bind:edges>
  <Background bgColor="#ccc" variant={BackgroundVariant.Dots} />
</SvelteFlow>
```

## Props

The type for props of `<Background />` component is exported as `BackgroundProps`.

<APIDocs componentName="Background" />
