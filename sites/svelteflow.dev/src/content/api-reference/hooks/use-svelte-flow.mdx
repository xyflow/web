---
description: The useSvelteFlow hook returns functions to update the viewport, transform
  coordinates or get node intersections for example.
---

# useSvelteFlow()

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/svelte/src/lib/hooks/useSvelteFlow.ts/#L24)

This hook returns functions to update the viewport, transform positions or get node intersections for example.

```svelte
<script lang="ts">
  import { useSvelteFlow } from '@xyflow/svelte';

  const { screenToFlowPosition, zoomIn } = useSvelteFlow();
</script>
```

## Signature

<APIDocs functionName="useSvelteFlow" />
