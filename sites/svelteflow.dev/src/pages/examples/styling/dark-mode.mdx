---
title: Dark Mode
description: Svelte Flow comes with a default style and a base style. This example shows how the base style looks.
---

import ExampleViewer from '@/components/example-viewer';
import ExampleLayout from '@/layouts/example-with-frontmatter';

<ExampleLayout>
Svelte Flow comes with a built-in dark mode. You can enable it by setting `colorMode="dark"`. The default is "light" and you can also use the system color mode by setting it to `colorMode="system"`.

<ExampleViewer codePath="examples/styling/dark-mode" />
</ExampleLayout>
