---
title: Add Node On Edge Drop
---

import { ExampleLayout } from 'xy-shared';
import { RemoteCodeViewer } from 'xy-shared';

<ExampleLayout>
You can create a new node when you drop the connection line on the pane by using the `onConnectionStart` and `onConnectionStop` handlers.

<RemoteCodeViewer
  source={`${process.env.NEXT_PUBLIC_EXAMPLES_URL}/svelte/examples/nodes/add-node-on-edge-drop/source.json`}
  preview={`${process.env.NEXT_PUBLIC_EXAMPLES_URL}/svelte/examples/nodes/add-node-on-edge-drop/index.html`}
  framework="svelte"
/>
</ExampleLayout>
