---
title: Updating Nodes
description: This example shows how to update the data field of a specific node.
---

import { ExampleLayout } from 'xy-shared';
import { RemoteCodeViewer } from 'xy-shared';
import { Callout } from 'nextra/components';

<ExampleLayout>
  You can update properties of nodes and edges freely.Don't forget to [self
  assign](https://svelte.dev/docs/svelte-components#script-2-assignments-are-reactive)
  `$nodes = $nodes` to trigger an update.
  <Callout type="info">
    You have to create a new `data` object on a node to notify Svelte Flow about
    data changes.
  </Callout>
  <br />

<RemoteCodeViewer
  source={`${process.env.NEXT_PUBLIC_EXAMPLES_URL}/svelte/examples/nodes/update-node/source.json`}
  preview={`${process.env.NEXT_PUBLIC_EXAMPLES_URL}/svelte/examples/nodes/update-node/index.html`}
  framework="svelte"
/>

</ExampleLayout>
