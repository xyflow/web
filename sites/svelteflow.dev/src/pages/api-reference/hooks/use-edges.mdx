---
title: useEdges
description: The useEdges hook returns an array of the current edges store.
---

import { PropsTable } from '@/components/props-table';
import { signature } from '@/references/hooks/useEdges.ts';

# useEdges

[Source on GitHub](https://github.com/xyflow/xyflow/blob/main/packages/svelte/src/lib/hooks/useNodesEdges.ts/#L8)

The `useEdges` hook returns an array of the current edges store.

```svelte
<script lang="ts">
  import { useEdges } from '@xyflow/svelte';

  const edges = useEdges();
</script>
```

## Signature

<PropsTable {...signature} />
