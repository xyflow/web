---

---

import getStaticPropsStats from '@/utils/get-static-props/github-npm-stats';

export async function getStaticProps() {
  const { stars, downloads } = await getStaticPropsStats();
  return {
    props: {
      ssg: { stars, downloads },
    },
  };
}

