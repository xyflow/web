---
title: Zoom Slider
---

import { Heading } from '@xyflow/xy-ui';
import { Steps, Tabs, Tab } from 'nextra/components';
import UiComponentViewer from '@/components/ui-component-viewer';
import getUiComponentConfig from '@/utils/get-static-props/ui-component-config';

export const getStaticProps = getUiComponentConfig('zoom-slider');

# Zoom Slider

A zoom control that lets you zoom in and out seamlessly using a slider.

<UiComponentViewer />

### Installation

```sh
npx shadcn add https://components.reactflow.dev/zoom-slider.json
```

### Usage

```jsx
import ZoomSlider from '@/components/ui/zoom-slider';

export default function() {
  return (
    <ReactFlow>
      <ZoomSlider>
    </ReactFlow>
  )
}
```
