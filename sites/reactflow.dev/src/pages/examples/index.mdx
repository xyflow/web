---
title: Feature Overview
description: A basic example showing the most used features of React Flow - controls, MiniMap, node types, edge types, edge labels, and custom styling.
---

import ExampleViewer from '@/components/example-viewer';
import ExampleLayout from '@/layouts/example-with-frontmatter';

<ExampleLayout>
This is a very basic example of a React Flow graph. On the bottom left you see the [Controls](/api-reference/components/Controls) and on the bottom right the [MiniMap component](/api-reference/components/MiniMap).
You can see different [node types](/api-reference/types/Node/) (input, default, output), [edge types](/api-reference/types/Edge) (bezier, step and smoothstep), edge labels and custom styled edge labels.

<ExampleViewer
  codePath="example-flows/Overview"
  additionalFiles={['initial-elements.js', 'CustomNode.jsx', 'overview.css']}
/>

</ExampleLayout>
