---
sidebar_position: 5
title: Touch Device
hide_table_of_contents: true
---

import CodeViewer from '@/components/code-viewer';
import ExampleLayout from '@/layouts/example';

<ExampleLayout>
You can connect nodes on a touch device by tapping two handles in a row. In this example we increased the size of the handles so that they are better tappable. You can disable this behavoir by setting the [`connectOnClick` prop](/docs/api/react-flow-props/#interaction) to `false`.

<CodeViewer
  codePath="example-flows/TouchDevice"
  additionalFiles={['index.css']}
/>
</ExampleLayout>
