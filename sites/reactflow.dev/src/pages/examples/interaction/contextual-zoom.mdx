---
sidebar_position: 10
title: Contextual Zoom
hide_table_of_contents: true
---

import ExampleViewer from '@/components/example-viewer';
import ExampleLayout from '@/layouts/example-with-frontmatter';

<ExampleLayout>
This example shows how the current zoom level can be used by a node to decide which content to show.
We are using selecting the zoom via the [`useStore` hook](/docs/api/hooks/use-store/#usestore) to update our custom node whenever the zoom changes.

<ExampleViewer
  codePath="example-flows/ContextualZoom"
  additionalFiles={['ZoomNode.js', 'index.css']}
/>
</ExampleLayout>
