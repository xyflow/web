---
title: Collision Detection
description: Find out if two nodes overlap each other
---

import { RemoteCodeViewer } from 'xy-shared';
import { ExampleLayout } from 'xy-shared';

<ExampleLayout>
This example shows a simple method to find out if two nodes overlap each other. It can be useful for interactively grouping or connecting nodes.

<RemoteCodeViewer
  source={`${process.env.NEXT_PUBLIC_EXAMPLES_URL}/react/examples/interaction/collision-detection/source.json`}
  preview={`${process.env.NEXT_PUBLIC_EXAMPLES_URL}/react/examples/interaction/collision-detection/index.html`}
  framework="react"
/>

</ExampleLayout>
