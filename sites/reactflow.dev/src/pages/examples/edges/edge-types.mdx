---
sidebar_position: 2
title: Edge Types
hide_table_of_contents: true
description: example showing different kinds of edge types in React Flow
---

import CodeViewer from '@/components/code-viewer';
import ExampleLayout from '@/layouts/example';

<ExampleLayout>
You can choose different kinds of [edge types](/docs/api/edges/edge-types/) in React Flow: `default` (bezier), `straight`, `step` and `smoothstep`. As you can see, you can define a type for each edge and mix them in one graph.

<CodeViewer codePath="example-flows/EdgeTypes" />
</ExampleLayout>
