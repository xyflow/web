---
sidebar_position: 2
title: Edge Types
hide_table_of_contents: true
---

import ExampleViewer from '@/components/example-viewer';
import ExampleLayout from '@/layouts/example';

<ExampleLayout>
Example that shows all the different [edge types](/docs/api/edges/edge-types/) of React Flow: `default` (bezier), `straight`, `step` and `smoothstep`. As you can see, you can define a type for each edge and mix them in one graph.

<ExampleViewer codePath="example-flows/EdgeTypes" additionalFiles={['utils.js']} />
</ExampleLayout>
