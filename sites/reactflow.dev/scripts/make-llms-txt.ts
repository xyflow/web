// Small version of the llms.txt file that only includes the most important sections of the docs as links.
// Uses nextra's getPageMap via the app's /api/page-map route (getPageMap only works inside Next.js context).

// TODO implement.
