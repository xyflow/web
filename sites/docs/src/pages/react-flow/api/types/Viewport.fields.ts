import { type PropsTableProps } from '@/components/props-table';

export const viewportFields: PropsTableProps = {
  props: [
    ['x', 'number'],
    ['y', 'number'],
    ['zoom', 'number'],
  ],
};
