import { PropsTable } from '@/components/props-table';
import { defaultEdgeOptionsFields } from '@/page-data/react-flow/api/types/DefaultEdgeOptions.fields.ts';

# DefaultEdgeOptions&lt;T>

[Source on GitHub](https://github.com/wbkd/react-flow/blob/f919cb152dd5a92ec3b4d7e4568ffe099bc2af39/packages/react/src/types/edges.ts#L73)

```ts
export type DefaultEdgeOptions<T> = {
  type?: string;
  animated?: boolean;
  hidden?: boolean;
  deletable?: boolean;
  selectable?: boolean;
  data?: T;
  selected?: boolean;
  markerStart?: string | EdgeMarker;
  markerEnd?: string | EdgeMarker;
  zIndex?: number;
  ariaLabel?: string;
  interactionWidth?: number;
  focusable?: boolean;
};
```

## Fields

<PropsTable {...defaultEdgeOptionsFields} />
