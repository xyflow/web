import { PropsTable } from '@/components/props-table';
import {
  defaultEdgeFields,
  smoothStepEdgeTypeFields,
  bezierEdgeTypeFields,
} from '@/page-data/react-flow/api/types/Edge.fields.ts';

# Edge&lt;T>

[Source on GitHub](https://github.com/wbkd/react-flow/blob/f919cb152dd5a92ec3b4d7e4568ffe099bc2af39/packages/react/src/types/edges.ts#L49)

```ts
export type Edge<T> =
  | DefaultEdge<T>
  | SmoothStepEdgeType<T>
  | BezierEdgeType<T>;
```

## Variants

### DefaultEdge&lt;T>

[Source on GitHub](https://github.com/wbkd/react-flow/blob/f919cb152dd5a92ec3b4d7e4568ffe099bc2af39/packages/react/src/types/edges.ts#L31)

<PropsTable {...defaultEdgeFields} />

### SmoothStepEdgeType&lt;T>

[Source on GitHub](https://github.com/wbkd/react-flow/blob/f919cb152dd5a92ec3b4d7e4568ffe099bc2af39/packages/react/src/types/edges.ts#L39C1-L39C1)

The `SmoothStepEdgeType` variant has all the same fields as the `DefaultEdge`
variant, but it also has the following additional fields:

<PropsTable {...smoothStepEdgeTypeFields} />

### BezierEdgeType&lt;T>

[Source on GitHub](https://github.com/wbkd/react-flow/blob/f919cb152dd5a92ec3b4d7e4568ffe099bc2af39/packages/react/src/types/edges.ts#L44)

The `BezierEdgeType` variant has all the same fields as the `DefaultEdge` variant,
but it also has the following additional fields:

<PropsTable {...bezierEdgeTypeFields} />
