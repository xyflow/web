{
  "name": "xyflow-nhost",
  "private": true,
  "scripts": {
    "dev": "nhost up",
    "stripe-webhook": "source .secrets && stripe listen --forward-to https://local.functions.local.nhost.run/v1/stripe/webhook --api-key $STRIPE_SECRET_KEY"
  },
  "dependencies": {
    "@nhost/nhost-js": "^4.7.0",
    "@upstash/redis": "^1.37.0",
    "cross-fetch": "^4.1.0",
    "express": "^4.22.1",
    "graphql-request": "^6.1.0",
    "jsonwebtoken": "^9.0.3",
    "node-mailjet": "^6.0.11",
    "stripe": "^12.18.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.25",
    "@types/jsonwebtoken": "^9.0.10"
  }
}
