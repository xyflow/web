---
sidebar_position: 4
title: Delete Edge on Drop
hide_table_of_contents: true
---

import CodeViewer from '@/components/code-viewer';

In this example we are showing how to delete an edge by using the [`onEdgeUpdate`, `onEdgeUpdateStart` and `onEdgeUpdateEnd` handlers](/docs/api/react-flow-props/#edges). If you drag an existing edge and drop it on the pane, it gets deleted from the `edges` array.

<CodeViewer codePath="example-flows/DeleteEdgeDrop" />
