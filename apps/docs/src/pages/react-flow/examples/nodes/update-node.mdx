---
title: UpdateNode
description: This example shows how to update the data field of a specific node. You need to create a new data object and always pass the newly created nodes array to the ReactFlow component.
---

import CodeViewer from '@/components/code-viewer';
import ExampleLayout from '@/components/example-layout';

<ExampleLayout>
This example shows how to update the data field of a specific node. Be aware that you need to create a new data object and always pass the newly created `nodes` array to the `ReactFlow` component.

<CodeViewer
  codePath="example-flows/UpdateNode"
  additionalFiles={['updatenode.css']}
/>
</ExampleLayout>
