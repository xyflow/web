---
title: Styled Components Light + Dark Mode
description: You can use any library you want to style your components. In this example we are using styled components to show how to create a simple dark / light mode.
---

import CodeViewer from '@/components/code-viewer';
import ExampleLayout from '@/components/example-layout';

<ExampleLayout>
In this example we are using [styled components](https://styled-components.com/) to show how to implement a dark and light mode.

<CodeViewer
  codePath="example-flows/StyledComponents"
  additionalFiles={['CustomNode.js', 'theme.js', 'nodes-edges.js']}
  dependencies={{
    'styled-components': 'latest',
  }}
/>
</ExampleLayout>
