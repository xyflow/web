---
title: Case Studies
description: Overview of case studies that use React Flow and Svelte Flow
---

import Link from 'next/link';
import { getPagesUnderRoute } from 'nextra/context';
import { RxCheckCircled } from 'react-icons/rx';

import BaseLayout from '@/layouts/base';
import HeroSection from '@/components/hero-section';
import ContentGrid, { ContentGridItem } from '@/components/content-grid';
import CaseStudyPreview from '@/components/case-study-preview';

export const Kicker = () => (
  <>
    <RxCheckCircled className="inline-block w-4 h-4 mr-1 text-react" />
    <span className="text-react">Success stories</span>
  </>
);

<BaseLayout>
  <HeroSection
    title="How Pro subscribers use React Flow to build"
    subtitle="Check how our users build custom node-based apps like workflow editors and diagramming tools"
    kicker={<Kicker />}
    align="center"
  />
  <ContentGrid className="mt-20">
    {getPagesUnderRoute('/case-studies').map((page) => {
      return (
        <ContentGridItem key={page.route}>
          <CaseStudyPreview
            title={page.frontMatter.title}
            description={page.frontMatter.description}
            client={page.frontMatter.client}
            route={page.route}
            className="px-8 py-16"
          />
        </ContentGridItem>
      );
    })}
  </ContentGrid>
</BaseLayout>
